#!/bin/zsh

precmd_vcs_info() {vcs_info}
precmd_functions+=( precmd_vcs_info )

setopt prompt_subst

autoload -Uz vcs_info

zstyle ':vcs_info:*' enable git

zstyle ':vcs_info:git:*' formats ' %F{red}%b%f %F{255}%r%f'

PROMPT='%F{240}%n@%m %F{blue}%1~${vcs_info_msg_0_} > '
